services:
  - type: web
    name: smartmoney-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        sync: false   # manuell in Render UI setzen
      - key: SUPABASE_URL
        value: https://lwanriaqqzgqslcendim.supabase.co
      - key: SUPABASE_ANON_KEY
        sync: false
